//@ts-nocheck

export function init() {
  const elem = document.querySelector('.code');
  // elem.style.width = window.innerWidth - 600 + 'px';
  // elem.style.height = window.innerHeight - 350 + 'px';
}
